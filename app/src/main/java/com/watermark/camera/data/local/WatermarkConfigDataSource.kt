package com.watermark.camera.data.local

class WatermarkConfigDataSource {

    suspend fun save(key: String, value: String) {
    }

    suspend fun read(key: String): String? {
        return null
    }
}
